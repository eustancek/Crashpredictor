version: 2
apps:
  crashpredictor:
    build_command: pip install -r requirements.txt
    start_command: gunicorn -w 2 -b 0.0.0.0:$PORT --timeout 30 api.app:application
    envs:
      HF_TOKEN: ${{ secrets.HUGGINGTOKEN }}
      PORT: "8080"
